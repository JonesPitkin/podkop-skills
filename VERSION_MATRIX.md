# Version Matrix

| Area | Coverage Policy | Source of Truth |
|---|---|---|
| `Podkop` package behavior | verify current package and wiki guidance before production changes | official `Podkop` repository and wiki |
| OpenWrt package flow | treat `opkg` and `apk` paths as version-sensitive | official OpenWrt documentation |
| `sing-box` dependency | verify minimum supported version for the specific Podkop branch | Podkop install and diagnostics material |
| repository release | current standalone repository release is `v1.0.0` | this repository |
